# list_operations

def filter_even_numbers(numbers):
    return [num 
            for num in numbers 
            if num % 2 == 0
            ]

# list_operations

def filter_odd_numbers(numbers):
    return [num 
            for num in numbers 
            if num % 2 != 0
            ]
